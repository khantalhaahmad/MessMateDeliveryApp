<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.messmate.delivery">

    <!-- 🌐 INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 📍 LOCATION -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MessMateDelivery"
        android:usesCleartextTraffic="true">

        <!-- 🔑 GOOGLE MAP API KEY -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB6sGl66iiA42kM5Ws1wWhWBHjzric6NMo" />

        <!-- 🚀 SPLASH -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MessMateDelivery">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔐 LOGIN -->
        <activity android:name=".ui.LoginActivity" />

        <!-- 📲 OTP -->
        <activity android:name=".ui.OtpActivity" />

        <!-- 🏠 MAIN -->
        <activity android:name=".ui.MainActivity" />

        <!-- 📦 ORDER DETAILS (MAP YAHI PE USE HOGA 🔥) -->
        <activity android:name=".ui.OrderDetailsActivity" />

    </application>

</manifest>